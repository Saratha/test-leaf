const browserVersion = "chrome";

function getBrowserVersion() {
    var browserVersion = "chrom 13.5";

    if (browserVersion === "chrome") {

    }

    console.log("browserVersion:", browserVersion);
}

getBrowserVersion();